<html lang='en'>
<head>
<title>Welcome Home</title>
<meta name="description" content="Shop for artwork by Gundogan Hagu.  Purchase canvas prints, framed prints, tapestries, posters, greeting cards, and more.  Biography coming soon...">
<meta name='viewport' content='width=device-width, initial-scale=1'>
<meta property='og:title' content="Gundogan Hagu - Art">
<meta property='og:type' content='website'>
<meta property='og:url' content='https://fineartamerica.com/profiles/gundogan-hagu'>
<meta property='og:image' content='https://fineartamerica.com/images/HeadshotDefault.jpg'>
<meta property='og:site_name' content='Fine Art America'>
<meta property='og:description' content="Shop for artwork by Gundogan Hagu.  Purchase canvas prints, framed prints, tapestries, posters, greeting cards, and more.  Biography coming soon...">
<meta property='fb:app_id' content='309028460694'>
<link rel='stylesheet' type='text/css' href='/css/stylesheet.css?v=58c3da5f9e'>
<link rel='stylesheet' type='text/css' href='/css/profiles/memberProfileNew.css?v=58c3da5f9e'>
<link rel='stylesheet' type='text/css' href='/css/profiles/searchEngine.css?v=58c3da5f9e'>


<link rel='alternate' type='application/rss+xml' title='New Artwork  - Gundogan Hagu' href='/rss/artwork/gundogan-hagu'>
<link rel='alternate' type='application/rss+xml' title='Fine Art Blog - Gundogan Hagu' href='/rss/blogs/gundogan-hagu'>
<link rel='alternate' type='application/rss+xml' title='Fine Art Events - Gundogan Hagu' href='/rss/events/gundogan-hagu'>
<link rel='canonical' href='https://fineartamerica.com/profiles/gundogan-hagu'>
 <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">



  


  
<script type='application/ld+json'>
{
  "@context":"http://schema.org",
  "@type":"Person",
  "name":"Gundogan Hagu",
  "image":"https://fineartamerica.com/images/HeadshotDefault.jpg",
  "url":"https://fineartamerica.com/profiles/gundogan-hagu",
  "homeLocation":{
    "@type":"Place",
    "address":{
      "@type":"PostalAddress",
      "addressLocality":"Jakarta",
      "addressRegion":"Punjab",
      "addressCountry":"Indonesia"
    }
  }
}
</script>


<style>

    #contentContainerDiv
    {
        
                        border-left: none;
                        border-right: none;
                        border-bottom: none;
                        background-color: transparent;
                        background-image: none;

                
    }

    .profiletitlebar > *:first-child
    {
        
                        padding-left: 0px;

                    }

    .flowdiv
    {
        
    }
	
	.arrange
	{
		margin: 20px 5px 15px 20px;
	}

</style>
<script type='text/javascript' src='/js/globalScripts.js?v=58c3da5f9e'></script>
<script type='text/javascript' src='/js/profiles/memberProfileNew.js?v=58c3da5f9e'></script>
<script type='text/javascript' src='/js/searchEngine/searchEngine.js?v=58c3da5f9e'></script>
<script type='text/javascript' src='/js/jquery191.js?v=58c3da5f9e'></script>
<script type='text/javascript'>

    var flagmemberloggedin = 1;
var memberIdType = 'artistid';
var memberId = 938523;

    var javascriptCurrentTag = "";
    var javascriptUrlProductType = "";
    var javascriptCurrentUrl = "";

    
    function confirmdelete(id)
    {
        result = confirm ('Are you sure you want to delete this piece of art?');

        if (result == true)
        {
            locationstring = "/profiles/gundogan-hagu?tab=artwork&deleteartworkid="+id + "&page=1";
            document.location = locationstring;
        }

    }

    function confirmdeleteevent(id)
    {
        result = confirm ('Are you sure you want to delete this event?');

        if (result == true)
        {
            locationstring = "/profiles/gundogan-hagu?deleteeventid="+id;
            document.location = locationstring;
        }

    }

    
</script>
</head>
<body>

<?php
include("header2.php");
?>




<section class="arrange" align="center">
<script type='application/ld+json'>
{
  "@context":"http://schema.org",
  "@type":"BreadcrumbList",
  "itemListElement":[
    {
      "@type":"ListItem",
      "position":1,
      "item":{
        "@id":"/",
        "name":"Home"
      }
    },
    {
      "@type":"ListItem",
      "position":2,
      "item":{
        "@id":"/artistdirectory.html",
        "name":"Artists"
      }
    },
    {
      "@type":"ListItem",
      "position":3,
      "item":{
        "@id":"/profiles/gundogan-hagu",
        "name":"Gundogan Hagu"
      }
    }
  ]
}</script>

<div id='profileContainerDiv' class='container flexContainer'>
<div id='memberfeaturesnewdiv'>
<a href="<?php echo base_url(); ?>index.php/Home/skillimg" class="btn btn-primary btn-lg active" role="button" aria-pressed="true">Upload Skill Image</a>
<a href="#" class="btn btn-secondary btn-lg active" role="button" aria-pressed="true">Upload Multiples Images</a>
<a href="<?php echo base_url(); ?>index.php/Home/profile" class="btn btn-primary btn-lg active" role="button" aria-pressed="true">Upload New Profile Picture</a>

</div>
</div>

<div id='topDiv' class='flexContainer'>

<div id='logoAndNameDiv'>
<?php if($user -> profile_photo){ ?>
<a href='#'><img id='topImage' src='<?php echo base_url();?>uploads/<?php echo $user->profile_photo ?>' class="img-thumbnail" alt='Gundogan Hagu - Artist' title='<?php echo $user->username; ?>'></a>

<?php } else{ ?>
<a href='#'><img id='topImage' src='<?php echo base_url(); ?>assets/img/user2.jpg' class="img-thumbnail" alt='Gundogan Hagu - Artist' title='<?php echo $user->username; ?>'></a>
<?php } ?>
<div id='topNameDiv'>
<h1> welcome <?php echo $user->username; ?> </h1>
<h2>Browse skills of <?php echo $user->username; ?>  </h2>
</div>

<div class="progress" style="margin-bottom: 60px; margin-top: 30px;">

  <div class="progress-bar" role="progressbar" style="width: 40%;" aria-valuenow="25" aria-valuemin="0" aria-valuemax="100">Your profile is 40% Complete</div>

</div>

<div class="btn-group" role="group" aria-label="Basic example">
  <button type="button" class="btn btn-primary">Follow</button>
  <button type="button" class="btn btn-dark">Contact</button>
  <button type="button" class="btn btn-primary">share</button>
</div>
<div class="btn-group" role="group" aria-label="Basic example">
  <button type="button" class="btn btn-primary">Send Message</button>
  <button type="button" class="btn btn-dark">Send Email</button>
 <a href="<?php echo base_url(); ?>index.php/Home/checkbio" class="btn btn-primary btn-lg active" role="button" aria-pressed="true">Check Bio</a>
</div>
</div>



<div id='memberDetailsDiv'>

<div id='memberStatsDiv'>

<div>
<p>Followers</p>
<p>0</p>
</div>
<div>
<p>Visitors</p>
<p>0</p>
</div>
</div>
</div>
<div class="btn-group" role="group" aria-label="Basic example">
 <a href="<?php echo base_url(); ?>index.php/Home/editbio" class="btn btn-primary btn-lg active" role="button" aria-pressed="true">Update Bio</a>

  <button type="button" class="btn btn-dark">Edit Headshot</button>
  <button type="button" class="btn btn-primary">Default Tab</button>
</div>
</div>

<script type='text/javascript'>pageLoad('artwork');</script>
<div id='tabAndContentContainerDiv'>
<section>
<a href="<?php echo base_url(); ?>index.php/Home/skillgallery" class="btn btn-primary btn-lg active" role="button" aria-pressed="true">Uploaded Images</a>
<button type="button" class="btn btn-dark">Achievements</button>
<button type="button" class="btn btn-primary">Skillsets</button>
<button type="button" class="btn btn-dark">Events</button>
<button type="button" class="btn btn-primary">Blogs</button>
<button type="button" class="btn btn-dark">Favourites</button>
<button type="button" class="btn btn-primary">Following</button>
<button type="button" class="btn btn-dark">Contests</button>

<button type="button" class="btn btn-primary">About</button>
<section>
<form action="<?php echo base_url();?>index.php/Home/logout" method="post"><input type="submit" class="btn btn-primary" value="logout"></form>
</section>

</section>
<div id='rightdiv'><div id='contentContainerDiv' data-tab='artwork'>
<script type='text/javascript'>

    function newHttpReq()
    {
        var newRequest = null;

        try
        {
            newRequest = new XMLHttpRequest();
        }
        catch(trymicrosoft)
        {
            try
            {
                newRequest = new ActiveXObject("Msxml2.XMLHTTP");
            }
            catch(othermicrosoft)
            {
                try
                {
                    newRequest = new ActiveXObject("Microsoft.XMLHTTP");
                }
                catch(failed)
                {
                    newRequest = null;
                }
            }
        }

        return newRequest;

    }

    var request = newHttpReq();
    var originalhtml = '';
    var originalposition = '';

    function initiatechange(id,position)
    {
        var url = 'queryartworknew.php?initiatechangeid=' + id + '&position=' + position;

        originalposition = id;
        originalhtml = document.getElementById('change[' + id + ']').innerHTML;

        document.getElementById('change[' + id + ']').innerHTML = "<img src='/assets/images/Searching.gif' style='display: inline-block;'>";

        if((request.readyState != 4) && (request.readyState != 0))
        {
            request.abort();
            request = newHttpReq();
        }

        request.open('GET',url,true);
        request.onreadystatechange = updateinitiatechange;
        request.send(null);

        return;

    }

    function updateinitiatechange()
    {
        //cancelposition();

        try
        {
            if(request.readyState == 4)
            {
            }

            if(request.status == 200)
            {
            }
        }
        catch (e)
        {
            return;
        }


        if (request.readyState == 4)
        {
            if(request.status == 200)
            {
                var response = request.responseText.split(",");
                originalposition = response[0];
                document.getElementById('change[' + originalposition + ']').innerHTML = response[1];
            }
        }
    }

    function cancelposition()
    {

        if(originalposition != '' && originalposition != null)
        {
            document.getElementById('change[' + originalposition + ']').innerHTML = originalhtml;
            originalposition = '';
            originalhtml = '';
        }
    }

    function changeposition(id)
    {
        var targetposition = document.getElementById('newposition[' + id + ']').value;

        var url = '../profiles/gundogan-hagu?tab=artwork&action=move&page=1&artworkid=' + id + '&targetposition=' + targetposition;

        document.location = url;

    }

    var fadecounter;
    var t;


    function detectsaveddiv()
    {

        if(document.getElementById('saveddiv'))
        {
            fadecounter = 100;
            setTimeout("fadeoutprogress()",2500);
        }
    }


    function fadeoutprogress()
    {

        var object = document.getElementById('saveddiv');
        object.style.filter = "alpha(opacity = " + fadecounter*1 + ")";
        object.style.opacity = fadecounter*0.01;

        fadecounter -= 1;

        if (fadecounter > 0)
        {
            t = setTimeout("fadeoutprogress()", 10);
        }
        else
        {
            object.parentNode.removeChild(object);
        }

    }

    function submitportfoliosearch()
    {
        document.getElementById('portfoliosearchform').submit();
    }


    //http://www.w3schools.com/jquery/jquery_syntax.asp
    $(document).ready(function() {

        var rowheights = [];

        function getheight(images, width)
        {
            width -= (images.length - 1) * 20;

            var h = 0;

            for(var i = 0; i < images.length; ++i)
            {
                h += $(images[i]).data('width') / $(images[i]).data('height');
            }

            return width / h;
        }

        function setheight(images, height)
        {
            rowheights.push(height);

            for(var i = 0; i < images.length; ++i)
            {
                $(images[i]).css({width: height * $(images[i]).data('width') / $(images[i]).data('height'),height: height});
                //$(images[i]).css(width, height * $(images[i]).data('width') / $(images[i]).data('height'));
                //$(images[i]).attr('src', $(images[i]).attr('src').replace(/w[0-9]+-h[0-9]+/, 'w' + $(images[i]).width() + '-h' + $(images[i]).height()));
            }
        }

        function resize(images, width)
        {
            setheight(images, getheight(images, width));
        }

        function run(max_height)
        {
            var widthcontainer = parseInt(document.getElementById('artworkFlowContainerDiv').offsetWidth);

            //alert(widthcontainer);

            // Get all of the images inside of #artworkFlowContainerDiv.
            var images = $('#artworkFlowContainerDiv .artworkFlowDiv');

            w: while(images.length > 0)
            {
                for(var i = 1; i < images.length + 1; i++)
                {
                    // Continue adding images to the "slice" array, and after you add each image, check the new minimum height of that slice of images
                    // by calling getheight.

                    var slice = images.slice(0, i);

                    var h = getheight(slice, widthcontainer);

                    if(h < max_height)
                    {
                        setheight(slice, h);

                        // Continue with the remaining images from index "i" on up.
                        images = images.slice(i);

                        continue w;
                    }
                }

                setheight(slice, Math.min(max_height, h));

                break;
            }

        }



        if(document.getElementById('artworkFlowContainerDiv'))
        {
            window.addEventListener('resize', function () { run(250); });
            run(250);
        }

    });


    function initializepopupobjects()
    {
        if(!document.getElementById('foregrounddiv') || !document.getElementById('backgrounddiv') || !document.getElementById('colordiv'))
        {
            var newdiv;
            var cssstyle;

            // Background Div
            if(!document.getElementById('backgrounddiv'))
            {
                newdiv = document.createElement('div');
                cssstyle = 'position: fixed; z-index: 5000; left: 0px; top: 0px; width: 10px; height: 10px; background-color: #000000; visibility: hidden; opacity: 0.50; filter: alpha(opacity=50);';
                newdiv.id = 'backgrounddiv';
                newdiv.setAttribute('style',cssstyle);
                newdiv.style.cssText = cssstyle;
                document.body.appendChild(newdiv);
            }

            // Foreground Div
            if(!document.getElementById('foregrounddiv'))
            {
                newdiv = document.createElement('div');
                cssstyle = 'position: fixed; z-index: 5001; left: 0px; top: 0px; width: 700px; min-height: 50px; padding: 40px; overflow: auto; border: 15px solid #444444; border-radius: 4px; background-color: #FFFFFF; visibility: hidden; box-shadow: 0px 0px 50px #000000;';
                newdiv.id = 'foregrounddiv';
                newdiv.setAttribute('style',cssstyle);
                newdiv.style.cssText = cssstyle;
                document.body.appendChild(newdiv);
            }
        }
    }


    function editimage(artworkid,positionid)
    {
        initializepopupobjects();

        var positiondivs = document.getElementsByClassName('flowpositiondiv');

        for(var i = 0; i < positiondivs.length; i++)
        {
            positiondivs[i].style.display = 'inline-block';
        }

        var backgroundobject = document.getElementById('backgrounddiv');
        backgroundobject.style.width = '100%';
        backgroundobject.style.height = '100%';
        backgroundobject.style.visibility = 'visible';

        var widthclient = parseInt(document.documentElement.clientWidth);
        var heightclient = parseInt(document.documentElement.clientHeight);

        //alert(widthclient);

        var widthtarget = parseInt(widthclient*0.80);
        var heighttarget = parseInt(heightclient*0.60);

        if((widthtarget + 110) > widthclient)
        {
            widthtarget = widthclient - 50 - 110;
        }

        if((heighttarget + 110) > heightclient)
        {
            heighttarget = heightclient - 50 - 110;
        }

        if(widthtarget > 250)
        {
            widthtarget = 250;
        }

        if(heighttarget > 250)
        {
            heighttarget = 250;
        }

        //var widthtarget = 800;
        //var heighttarget = 300;

        var offsetleft = 0;
        var offsettop = 0;

        var offsetx = -30;
        var offsety = 0;

        if(document.documentElement.scrollTop && !document.body.scrollTop)
        {
            //offsetleft = document.documentElement.scrollLeft;
            //offsettop = document.documentElement.scrollTop;
        }
        else
        {
            //offsetleft = document.body.scrollLeft;
            //offsettop = document.body.scrollTop;
        }

        var foregrounddiv = document.getElementById('foregrounddiv');
        foregrounddiv.style.width = widthtarget + 'px';
        foregrounddiv.style.height = heighttarget + 'px';
        foregrounddiv.style.visibility = 'visible';
        foregrounddiv.innerHTML = "<img src='/assets/images/Searching.gif' style='display: inline-block;'>";

        foregrounddiv.style.left = (offsetx + offsetleft + parseInt((widthclient - (widthtarget + 50)) / 2)) + 'px';
        foregrounddiv.style.top = (offsety + offsettop + parseInt((heightclient - (heighttarget + 50)) / 2)) + 'px';

        var localvalue = requestcount;

        httpobject[localvalue] = newHttpReq();
        httpobject[localvalue].onreadystatechange = function(){

            try
            {
                if(httpobject[localvalue].readyState == 4)
                {
                }

                if(httpobject[localvalue].status == 200)
                {
                }
            }
            catch (e)
            {
                return;
            }

            if(httpobject[localvalue].readyState == 4 && httpobject[localvalue].status == 200)
            {
                document.getElementById('foregrounddiv').innerHTML = httpobject[localvalue].responseText;
            }


        };

        var url = '../queries/queryprofilepageeditimage.php';
        var poststring = 'action=refresh&artworkid=' + artworkid + '&positionid=' + positionid;

        httpobject[localvalue].open('post',url,true);
        httpobject[localvalue].setRequestHeader('content-type', 'application/x-www-form-urlencoded');
        httpobject[localvalue].send(poststring);

        requestcount += 1;

        return;

    }


    function hideeditimage()
    {
        var positiondivs = document.getElementsByClassName('flowpositiondiv');

        for(var i = 0; i < positiondivs.length; i++)
        {
            positiondivs[i].style.display = 'none';
        }

        hideforegrounddiv();
    }


    function hideforegrounddiv()
    {
        document.getElementById('backgrounddiv').style.visibility = 'hidden';
        document.getElementById('foregrounddiv').style.visibility = 'hidden';
        document.getElementById('foregrounddiv').innerHTML = '&nbsp;';
    }

    function initiatechangenew(id,position)
    {
        var url = 'queryartworkflow.php?initiatechangeid=' + id + '&position=' + position;

        originalposition = id;
        originalhtml = document.getElementById('changepositiondivnew').innerHTML;

        document.getElementById('changepositiondivnew').innerHTML = "<img src='/assets/images/Searching.gif' style='display: inline-block;'>";

        if((request.readyState != 4) && (request.readyState != 0))
        {
            request.abort();
            request = newHttpReq();
        }

        request.open('GET',url,true);
        request.onreadystatechange = updateinitiatechangenew;
        request.send(null);

        return;

    }

    function updateinitiatechangenew()
    {
        //cancelposition();

        try
        {
            if(request.readyState == 4)
            {
            }

            if(request.status == 200)
            {
            }
        }
        catch (e)
        {
            return;
        }


        if (request.readyState == 4)
        {
            if(request.status == 200)
            {
                var response = request.responseText.split(",");
                originalposition = response[0];
                document.getElementById('changepositiondivnew').innerHTML = response[1];
            }
        }
    }

    function cancelpositionnew()
    {

        if(originalposition != '' && originalposition != null)
        {
            document.getElementById('changepositiondivnew').innerHTML = originalhtml;
            originalposition = '';
            originalhtml = '';
        }
    }

</script>

<div class='profilecontentdiv'><div style='display: inline-block; width: 100%; text-align: center;'>
<div id='artworkFlowContainerDiv'>
</div>
</div></div>
<div style='float: left; width: 100%; border-top: 1px solid #CCCCCC;'>
</div>
</div></div>
</div>
</div></div>
</div>
    
    <script type='text/javascript' src='/js/lazySizes.js?v=58c3da5f9e4'></script>
    <script type='text/javascript'>
    
        var rowheights = [];

        function getheight(images, width)
        {
            //width -= images.length * 20;
            width -= (images.length - 1) * 30;
            
            var h = 0;

            for(var i = 0; i < images.length; ++i)
            {
                h += $(images[i]).data('width') / $(images[i]).data('height');
            }

            return width / h;
        }

        function setheight(images, height)
        {
            rowheights.push(height);

            for(var i = 0; i < images.length; ++i)
            {
                var newWidth = height * $(images[i]).data('width') / $(images[i]).data('height');
                var newHeight = height;
                
                $(images[i]).css({width: newWidth});
                
                // This is not actually necessary.
                $(images[i]).find('.flowImage').css({width: newWidth, height: newHeight});                
                
                //$(images[i]).css({width: height * $(images[i]).data('width') / $(images[i]).data('height'),height: height});
                //$(images[i]).css(width, height * $(images[i]).data('width') / $(images[i]).data('height'));
                //$(images[i]).attr('src', $(images[i]).attr('src').replace(/w[0-9]+-h[0-9]+/, 'w' + $(images[i]).width() + '-h' + $(images[i]).height()));
            }
        }

        function resize(images, width)
        {
            setheight(images, getheight(images, width));
        }

        function run(max_height)
        {
            var widthContainer = parseInt(document.getElementById('imageFlowContainerDiv').offsetWidth);
            var images = $('#imageFlowContainerDiv .flowDiv');

            w: while(images.length > 0)
            {
                for(var i = 1; i < images.length + 1; i++)
                {
                    // Continue adding images to the "slice" array, and after you add each image, check the new minimum height of that slice of images
                    // by calling getheight.

                    var slice = images.slice(0, i);

                    var h = getheight(slice, widthContainer);

                    if(h < max_height)
                    {
                        setheight(slice, h);

                        // Continue with the remaining images from index "i" on up.
                        images = images.slice(i);

                        continue w;
                    }
                }

                setheight(slice, Math.min(max_height, h));

                break;
            }

        }

                if(document.getElementById('imageFlowContainerDiv'))
        {
            var targetFlowDivHeight = 350;
        
            window.addEventListener('resize', function () { run(targetFlowDivHeight); });
            run(targetFlowDivHeight);
        }
            
    
    </script>
    

</section>


<?php
include("footer.php");
?>
 <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js" integrity="sha384-9/reFTGAW83EW2RDu2S0VKaIzap3H66lZH81PoYlFhbGU+6BZp6G7niu735Sk7lN" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js" integrity="sha384-B4gt1jrGC7Jh4AgTPSdUtOBvfO8shuf57BaghqFfPlYxofvL8/KUEfYiJOMMV+rV" crossorigin="anonymous"></script>


</body>
</html>